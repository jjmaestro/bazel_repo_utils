load("@aspect_bazel_lib//lib:docs.bzl", "stardoc_with_diff_test", "update_docs")

package(default_visibility = ["//:__subpackages__"])

stardoc_with_diff_test(
    name = "index",
    bzl_library_target = "@repo_utils//lib:index",
    renderer = "//:renderer",
)

update_docs(name = "update")
